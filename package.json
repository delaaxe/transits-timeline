{
  "name": "transits-timeline",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Astrological transit timeline. The app itself is index.html; these scripts only test and check it.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test \"test/*.test.mjs\"",
    "typecheck": "tsc -p tsconfig.json",
    "check": "npm test && npm run typecheck && npm run build",
    "deploy": "./scripts/deploy-cloudflare-pages.sh"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "5.7.2"
  }
}
